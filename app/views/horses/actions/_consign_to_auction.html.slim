- classes = single_action ? "btn btn-light btn-sm border border-success text-success text-decoration-none" : dropdown_classes
= link_to t("horse.actions.consign_to_auction"), new_horse_auction_consignment_path(object), class: classes, data: { turbo_frame: "modal" }
