- content_for :breadcrumbs do
  = render Shared::BreadcrumbsComponent.new(links: [{ name: stable.name, href: current_stable_path },
    { name: t("common.edit"), href: "#" }])

= turbo_frame_tag stable do
  = render "form", form: stable_form, url: update_current_stable_path
