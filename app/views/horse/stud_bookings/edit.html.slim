- title = t(".title", name: @horse.name)
- content_for :title, title

.text-center
  h1.text-xl.font-bold= title

= render "form", stud: @horse, booking: @booking, submit_text: t("common.actions.update"), url: horse_stud_booking_path(@horse, @booking), http_method: :put
