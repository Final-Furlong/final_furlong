on:
  workflow_call:

env:
  CI: true
  MAILTRAP_API_KEY: foo

jobs:
  linters:
    name: Linters
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Ruby and install gems
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Yarn install
        run: yarn --prefer-offline

      - name: RuboCop
        run: |
          bundle exec rubocop

      - name: Slim Lint
        run: |
          bundle exec slim-lint

      - name: Fasterer
        run: |
          bin/fasterer

      - name: EsLint
        run: |
          yarn run eslint app/javascript

      - name: StyleLint
        run: |
          yarn run stylelint app/assets/tailwind

      - name: Locales
        run: |
          bin/i18n-tasks health
