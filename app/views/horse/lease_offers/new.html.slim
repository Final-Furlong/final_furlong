ruby:
  horse = @lease_offer.horse
  content_for :title, t(".title")

.text-center
  h1.text-xl.font-bold= t(".title")
  .alert.alert-info
    div
      .text-sm= t(".duration.#{horse.status}")
      .text-sm= t(".rules.#{horse.status}")

= render "form", lease_offer: @lease_offer, horse: horse
