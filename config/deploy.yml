# Name of your application. Used to uniquely configure containers.
service: final_furlong

# Name of the container image.
image: equestrianerd/final-furlong

# Deploy to these servers.
servers:
  web:
    - <%= ENV["PRODUCTION_IP"] %>
  job:
    hosts:
      - <%= ENV["PRODUCTION_IP"] %>
    cmd: bundle exec good_job start

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
proxy:
  ssl: true
  host: app.finalfurlong.org
  # Proxy connects to your container on port 80 by default.
  # app_port: 3000
  healthcheck:
    path: /health/upstream
    interval: 2
    timeout: 10

# Credentials for your image host.
registry:
  username: equestrianerd
  # Specify the registry server, if you're not using Docker Hub
  # server: registry.digitalocean.com / ghcr.io / ...
  # username: my-user

  # Always use an access token rather than real password (pulled from .kamal/secrets).
  password:
    - KAMAL_REGISTRY_PASSWORD

# Configure builder setup.
builder:
  arch: arm64
  # Pass in additional build args needed for your Dockerfile.
  # args:
  #   RUBY_VERSION: <%= ENV["RBENV_VERSION"] || ENV["rvm_ruby_string"] || "#{RUBY_ENGINE}-#{RUBY_ENGINE_VERSION}" %>

logging:
  options:
    max-size: 100m

# Inject ENV variables into containers (secrets come from .kamal/secrets).
#
env:
  clear:
    POSTGRES_USER: final_furlong
    POSTGRES_DB: final_furlong_production
    RAILS_LOG_TO_STDOUT: true
  secret:
    - RAILS_MASTER_KEY
    - POSTGRES_PASSWORD

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal app logs -r job" will tail logs from the first server in the job section.
#
# aliases:
#   shell: app exec --interactive --reuse "bash"
aliases:
  # Interactive sessions
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  dbc: app exec --interactive --reuse "bin/rails dbconsole"

  # Logs
  logs: app logs -f

  # Deployment verification
  verify: app exec --roles=web "bin/rails deploy:verify"

  # Database operations
  db-health: app exec --roles=web "bin/rails db:health"
  integrity: app exec --roles=web "bin/rails integrity:check"

  # Operational tasks
  clear-cache: app exec --roles=web "bin/rails ops:clear_cache"
  env-check: app exec --roles=web "bin/rails ops:env_check"
  pending: app exec --roles=web "bin/rails ops:pending_migrations"
  warm-cache: app exec --roles=web "bin/rails ops:warm_cache"

# Use a different ssh user than root
#
ssh:
  user: <%= ENV["SSH_USER"] %>
  port: <%= ENV["SSH_PORT"] %>

# Use a persistent storage volume.
#
volumes:
  - "final_furlong_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
#
asset_path: /rails/public/assets

# Configure rolling deploys by setting a wait time between batches of restarts.
#
# boot:
#   limit: 10 # Can also specify as a percentage of total hosts, such as "25%"
#   wait: 2

# Use accessory services (secrets come from .kamal/secrets).
#
accessories:
  postgres:
    image: postgres:18
    host: <%= ENV["PRODUCTION_IP"] %>
    port: "5432:5432"
    env:
      clear:
        POSTGRES_USER: final_furlong
        POSTGRES_DB: final_furlong_production
        DB_HOST: final_furlong-postgres
      secret:
        - POSTGRES_PASSWORD
    directories:
      - data:/var/lib/postgresql/data
  # redis:
  # image: valkey/valkey:8
  # host: 192.168.0.2
  # port: 6379
  # directories:
  # - data:/data
