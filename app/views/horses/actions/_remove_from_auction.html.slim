- horse = object.auction_horse
= button_to t("horse.actions.remove_from_auction"), auction_horse_path(horse.auction, horse), class: classes, method: :delete,
        data: { turbo_confirm: t("horse.auction_consignments.destroy.confirm"),
                turbo_confirm_title: t("horse.auction_consignments.destroy.confirm_title", auction: horse.auction.title), turbo_frame: "_top" }
