p= t(".greeting", recipient: @resource.email)

p= t(".instruction")

p
  = render Ui::Link::Component.new(url: edit_password_url(@resource, reset_password_token: @token), data: { turbo_frame: "_top" }) do
    = t(".action")

p= t(".instruction_2")
p= t(".instruction_3")
