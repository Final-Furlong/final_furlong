= turbo_stream.append "horse-list" do
  = render partial: "schedule_horse", collection: horses.includes(:training_schedule), as: :horse, locals: { schedule:  }

= render "shared/loading", pagy: @pagy
