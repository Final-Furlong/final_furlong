- content_for :breadcrumbs do
  = render Shared::BreadcrumbsComponent.new(title: t("stables.index.all"))

= turbo_frame_tag "stables" do
  - stables.in_groups_of(4) do |list|
    .row.mb-3
      - list.each do |stable|
        .col-sm-3
          = render stable if stable
