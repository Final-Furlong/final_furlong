- classes = single_action ? "btn btn-outline btn-error btn-outline btn-sm" : dropdown_classes
= link_to t("horse.actions.lease_offer.cancel"), horse_lease_offer_path(object), class: classes,
        data: { turbo_method: :delete, turbo_confirm: t("common.confirm.title"),
                confirm_details: t("horse.lease_offers.destroy.confirm") }
