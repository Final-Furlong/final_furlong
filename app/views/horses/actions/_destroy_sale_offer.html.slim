= button_to t("horse.actions.sale_offer.cancel"), horse_sale_offer_path(object), class: classes,
        method: :delete,
        data: { turbo_confirm: t("horse.sale_offers.destroy.confirm"),
                turbo_confirm_title: t("horse.sale_offers.destroy.confirm_title") }
