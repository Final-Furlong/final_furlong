= button_to t("horse.actions.lease_offer.reject"), horse_lease_offer_path(object), class: classes, method: :delete,
        data: { turbo_confirm: t("horse.lease_offers.reject.confirm"),
                turbo_confirm_title: t("horse.lease_offers.reject.confirm_title") }
