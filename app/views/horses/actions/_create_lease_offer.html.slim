- classes = single_action ? "btn btn-light btn-sm border border-success text-success text-decoration-none" : ""
= link_to t("horse.actions.create_lease_offer"), new_horse_lease_offer_path(object), class: classes, data: { turbo_frame: "modal" }
